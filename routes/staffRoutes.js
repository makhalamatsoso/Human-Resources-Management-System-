const express = require('express');
const Staff = require('../models/staff');  // Import Staff model
const router = express.Router();

// Define CRUD operations for staff (similar to vehicleRoutes)

module.exports = router;
